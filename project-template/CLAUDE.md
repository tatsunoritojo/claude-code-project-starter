# {{PROJECT_NAME}}

{{ONE_LINE_DESCRIPTION}}

<!--
このファイルは新規プロジェクト用の雛形です。{{...}} を実際の値に置き換えて使ってください。
グローバルルール（~/.claude/CLAUDE.md）は全セッションに自動適用されるため、ここには再掲しません。
このファイルには「このプロジェクト固有」の情報だけを書きます。
-->

## 次セッション着手用
- 現在地:
- 次アクション:
- 参照ファイル:
- 未解決 / 別扱い:
- 最終更新:

<!-- グローバル「セッション終了時の規律」に従い、非軽微な進捗があったセッションを終了するときは必ずこの欄を更新する -->

## 技術スタック

{{TECH_STACK_LIST}}

## 主要エントリポイント

- {{ENTRY_POINT_1}}
- {{ENTRY_POINT_2}}

## 開発コマンド

{{COMMAND_HINT_OR_POINTER}}
（詳細は `package.json` / `pyproject.toml` 等を参照。ここに重複させない）

## ディレクトリ構造

```
{{DIRECTORY_TREE_TOP_LEVEL}}
```

## このプロジェクト固有の作業ルール

{{PROJECT_SPECIFIC_RULES_PLACEHOLDER}}

<!--
グローバルの Green/Yellow/Red 実行ポリシーはそのまま適用される。
ここには「このプロジェクトでは特にこの操作を Red 扱いにする」のような
プロジェクト固有の上書き・追加だけを書く。
-->

## 運用設計（非自明な運用がある場合のみ）

<!--
マルチアカウント運用・情報境界・手動同期パイプライン・隔離実行環境など、
コードからは読み取れない運用設計を持つ場合に記載する。通常の単一リポ運用なら丸ごと削除してよい。

記載例:
- 設計パターン名（隔離実行環境 / 一方向パイプライン 等）
- 関連リポと役割
- 禁止判断のリスト（AI が提案しそうな間違った判断 — 「統合整理」「不足情報の補完」など）
-->

## このプロジェクトの完了判定（Definition of Done）

このプロジェクトで「完了」と呼ぶための条件:

- [ ] build / lint / test がすべて通る（グローバル CLAUDE.md「完了判定」の必要条件）
- [ ] {{PROJECT_DOD_REAL_ENV_CHECK}}
  <!-- 例: 本番環境で該当機能の挙動を確認 -->
- [ ] {{PROJECT_DOD_DOMAIN_SPECIFIC}}
  <!-- 案件固有の確認項目。空欄なら削除 -->

テストがカバーしていない経路（migration、外部連携、本番固有設定、ログ設定など）を意識的に書き出す。

## 関連ドキュメント

- 概要: `docs/01-overview.md`
- 設計判断（ADR）: `docs/decisions/`
- {{OTHER_DOCS}}
